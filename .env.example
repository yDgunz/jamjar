# Root directory for all data (input, output, database)
# JAM_DATA_DIR=.

# SQLite database path (relative to JAM_DATA_DIR or absolute)
# JAM_DB_PATH=jam_sessions.db

# Directory for uploaded audio files (relative to JAM_DATA_DIR or absolute)
# JAM_INPUT_DIR=input

# Directory for exported tracks (relative to JAM_DATA_DIR or absolute)
# JAM_OUTPUT_DIR=output

# Comma-separated list of allowed CORS origins
# JAM_CORS_ORIGINS=http://localhost:5173

# API server port
# JAM_PORT=8000

# Secret key for signing JWT tokens (generate with: openssl rand -hex 32)
JAM_JWT_SECRET=
# API key for CLI upload authentication (generate with: openssl rand -hex 32)
JAM_API_KEY=

JAM_R2_ACCOUNT_ID=
JAM_R2_ACCESS_KEY_ID=
JAM_R2_SECRET_ACCESS_KEY=
JAM_R2_BUCKET=